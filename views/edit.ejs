<%- include('partials/header.ejs') %>

    
    <table>
        <tr>
            <th>Name</th>
            <th>Contact</th>
            <th>edit</th>
            <th>delete</th>
        </tr>
        <% for(let i = 0; i < items.length; i++ ){ %>
            <tr>
              <td><%= items[i].name %></td>
              <td><%= items[i].phno %></td>
              <td><%= items[i].phno %></td>
                <td><a href="/delete/<%= items[i]._id %>">delete</a></td>
              <td><a href="/editdata/<%= items[i]._id %>">edit</a></td>
            </tr>
            <% } %>
            
            <tr>
            </table>
        
    </body>
    </html>

